<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org">

	<select id="selectOrgInfo" parameterType="long" resultType="OrganizationDTO">                  
		
		select 	seq,
				organizationCode,
				organizationName,
				parentCode,
				parentSeq,
				status
		from 	organizationtype 
		WHERE 	parentSeq = #{seq}
		<!-- 
		WHERE 	parentSeq = ${id}
		 -->
		                       
	</select>
	
	<select id="selectHierary_chk" parameterType="long" resultType="int">     

		select 	COUNT(*) as cnt
		from 	organizationtype 
		WHERE 	parentSeq = #{seq}
		        
	</select>	
		

</mapper>